<Window x:Class="MyNet8AppLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="MyNet8App Launcher"
        Height="450" Width="600"
        MinHeight="400" MinWidth="500">

    <Grid Margin="12">
        <!-- 4 rows: inputs, mode, run‑button, log -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Date range -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Label Content="Start date:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker x:Name="StartDatePicker"
                        SelectedDate="{x:Static sys:DateTime.Today}"
                        Margin="0,0,8,0"/>
            <Label Content="End date:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker x:Name="EndDatePicker"
                        SelectedDate="{x:Static sys:DateTime.Today}"/>
        </StackPanel>

        <!-- Mode selector -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Label Content="Mode:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="ModeComboBox" Width="140" SelectedIndex="0">
                <ComboBoxItem Content="Reports"     Tag="reports"/>
                <ComboBoxItem Content="Support data" Tag="support"/>
                <ComboBoxItem Content="Both"        Tag="both"/>
            </ComboBox>
        </StackPanel>

        <!-- Run button -->
        <Button x:Name="RunButton" Grid.Row="2"
                Content="Run" Height="32" Width="120"
                HorizontalAlignment="Left"
                Margin="0,0,0,8"
                Click="RunButton_Click"/>

        <!-- Log output -->
        <TextBox x:Name="LogTextBox" Grid.Row="3"
                 FontFamily="Consolas" FontSize="12"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"/>
    </Grid>
</Window>
